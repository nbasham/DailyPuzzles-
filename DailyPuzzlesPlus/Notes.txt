TODO
Change logo to DailyPuzzles+?
Factoids needs a big Tap Here target
Should we remove CloudKitDataReceivedNotification?
Add half ball to factoid
                ZStack(alignment: .topLeading) {
                    Circle()
                        .frame(height: viewModel.bottomViewHeight)
                        .position(x: -viewModel.bottomViewHeight, y: viewModel.bottomViewHeight/2)
                }

Remember
When uploading to app store changed name from DailyPuzzles+ to DailyPuzzlesPlus
